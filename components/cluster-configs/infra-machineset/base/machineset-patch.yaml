apiVersion: machine.openshift.io/v1beta1
kind: MachineSet
metadata:
  labels:
    cluster-api/role: "infra"
spec:
  template:
    spec:
      metadata:
        labels:
          node-role.kubernetes.io/infra: ""
          cluster-api/role: "infra"
      taints:
        - key: node-role.kubernetes.io/infra
          effect: NoSchedule
